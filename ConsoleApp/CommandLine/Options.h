#pragma once

enum class Command
{
    Convert,
    Help,
    Unknown
};

enum class ImageConverterOption
{
    InputFile,
    OutputFile,
    OutputFormat,
    Filter,
    Help,
    Unknown
};
